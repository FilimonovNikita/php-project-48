<?php
/*
namespace Differ\Formatter;

$autoloadPathCloud = __DIR__ . '/Formatters/Json.php';
require_once $autoloadPathCloud;

use function Differ\Formatters\Json\format;

function getFormat($data, $format)
{
    switch ($format) {
        case "json":
            return format($data);
        default:
            throw new \Exception("Unknown format: {$format}");
    }
}*/